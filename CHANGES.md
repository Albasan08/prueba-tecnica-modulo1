1. Añadir clases a los elementos de HTML, ya que CSS trabaja con clases mientras que JS trabaja con IDs. Cambiar los selector de #(ids) por .(clases) en el documento de style.css
    - Clase 1: .list

2. Cambiar el nombre del archivo styles.css por main.css ya que se nombra así por conveniencia. Los archivos css llamados styles.css se refieren a estilos externos que se pueden usar como librerías.
Cambiar el enlace en el documento html en la etiqueta href.
Cambiar el nombre del documento de app.js por index.js por la misma razón.

3. Añadir etiquetas para complementar el código en HTML.
    - Separar el contenido por etiquetas section (input y lista).
    - Añadir etiqueta header para separar h1.
    - Añadir etiqueta form y type="submit" al botón para que se dispare el botón del evento.
    - Añadir etiqueta name y type al input.

4. Cambiar el formato de las funciones para aumentar legibilidad.

5. Poner en el orden correcto las diferentes partes del documento de JS. Como se ha cambiado el orden es necesario "recoger" el documento y ponerlo a la escucha para garantizar que el DOM esté completamente cargado antes de poder acceder a los elementos.

6. División de responsabilidades.

7. Creación de nodos y fragmento.

8. Uso de querySelector en vez de getElement para utilizar métodos más actuales.